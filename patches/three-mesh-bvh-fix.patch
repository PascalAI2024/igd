--- node_modules/three-mesh-bvh/src/utils/ExtensionUtilities.js
+++ node_modules/three-mesh-bvh/src/utils/ExtensionUtilities-fixed.js
@@ -3,7 +3,9 @@
 import { MeshBVH } from '../core/MeshBVH.js';
 import * as THREE from 'three';
 
-const BatchedMesh = THREE.BatchedMesh || null; // this is necessary to not break three.js r157-
+// Temporary fix for build issue
+// const BatchedMesh = THREE.BatchedMesh || null; // this is necessary to not break three.js r157-
+const BatchedMesh = null; // Fixed version to prevent build errors
 const IS_REVISION_166 = parseInt( REVISION ) >= 166;
 const ray = /* @__PURE__ */ new Ray();
 const direction = /* @__PURE__ */ new Vector3();